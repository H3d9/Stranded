VS设置

-----------

解决方案选项设置为：```Debug x86```

编译时，需要将```StrandedEngine```的附加包含目录设置为DirectX 9.0c SDK的include目录。



使用Stranded引擎库

---

因为没有使用```Makefile``` ，故应当将每个子文件夹都添加到“包含目录”以供编译器索引。

对于用户，仅需将暴露的接口所在的头文件添加到“包含目录”即可，此后用户仅需在其工程中引用```Engine/Engine.h``` 即可。